# Environment Variables
# Copy this file to .env and fill in your values
# NEVER commit .env to git!

# ── Odoo Connection (REQUIRED) ───────────────────────────────────
ODOO_URL=https://fracktal.odoo.com/
ODOO_DB=fracktal
ODOO_USERNAME=suresh@fracktal.in
ODOO_PASSWORD=1a64881339bcd77ef5bc6eb1c7becc6ab214e0b0


# ── Reminder Settings ────────────────────────────────────────────
COMPANY_NAME=fracktal works
REMINDER_FROM_EMAIL=suresh@fracktal.in
REMINDER_SENDER_NAME=Accounts Receivable
REMINDER_HOUR=10
REMINDER_MINUTE=0

# ── SMTP (Optional — only if using --smtp flag) ─────────────────
# If omitted, emails are sent through Odoo's built-in mail system
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=@gmail.com
SMTP_PASSWORD=your-app-password

# ── LLM Keys (Optional — only for standalone mode) ──────────────
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
